export interface ICourse {
    id: number;
    id_study_plan: number;
    name: string;
    code: string;
    cycle: string;
    credits: number;
    is_active: boolean;
    theoretic_hours: string;
    practice_hours: string;
}